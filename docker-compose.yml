services:
  flight:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: flight_dev
    # stdin_open: true
    # tty: true
    volumes:
      - .:/app
    networks:
      - axrail

networks:
  axrail:
    name: axrail-test
    driver: bridge
